* text=auto eol=lf
*.ps1 text eol=crlf
*.zip binary
*.exe binary

# Signed manifests and payload hashes cover the exact publisher bytes.
# Never normalize line endings or otherwise transform this channel.
site/stable/** -text
